{
  "openapi": "3.0.0",
  "info": {
    "title": "eBay Browse API (Lite - Search Only)",
    "version": "v1",
    "description": "Simplified API definition for searching items on eBay."
  },
  "paths": {
    "/item_summary/search": {
      "get": {
        "operationId": "searchItems",
        "summary": "Search for items by keyword with advanced filtering.",
        "parameters": [
          {
            "name": "q",
            "in": "query",
            "description": "The search keywords. Example: 'iphone 12', 'lego star wars'.",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "filter",
            "in": "query",
            "description": "Comma-separated filters. YOU MUST USE THIS SYNTAX:\n1. Price Range: 'price:[MIN..MAX]' (e.g., 'price:[50..150]').\n2. Currency: 'priceCurrency:USD' (or EUR, GBP, PLN).\n3. Buying Options: 'buyingOptions:{FIXED_PRICE}' (Buy It Now) or 'buyingOptions:{AUCTION}' (Auctions).\n4. Condition: 'conditionIds:{1000}' (New), 'conditionIds:{3000}' (Used), 'conditionIds:{1000|3000}' (Both).\n\nEXAMPLE FULL FILTER: 'price:[10..100],priceCurrency:USD,buyingOptions:{FIXED_PRICE},conditionIds:{3000}'",
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "sort",
            "in": "query",
            "description": "Sort order. Options: 'price' (lowest first), '-price' (highest first), 'newlyListed', 'distance'.",
            "schema": {
              "type": "string",
              "enum": ["price", "-price", "newlyListed", "distance"]
            }
          },
          {
            "name": "limit",
            "in": "query",
            "description": "Max items to return (default 10).",
            "schema": {
              "type": "integer",
              "default": 10
            }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "itemSummaries": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "title": { "type": "string" },
                          "price": {
                            "type": "object",
                            "properties": {
                              "value": { "type": "string" },
                              "currency": { "type": "string" }
                            }
                          },
                          "itemWebUrl": { "type": "string" }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}